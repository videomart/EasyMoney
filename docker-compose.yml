services:
  easymoney:
    build: .
    ports:
      - "3002:3002"
    volumes:
      - easymoney-data:/app/data
    environment:
      - PORT=3002
    restart: unless-stopped

volumes:
  easymoney-data:
